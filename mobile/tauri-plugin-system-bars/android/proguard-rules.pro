# No obfuscation rules needed: the plugin is a thin wrapper over
# WindowInsetsControllerCompat and holds no reflection targets beyond the
# Tauri annotation processor, which tauri-android already keeps.
